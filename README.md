# Github Classroom Template Initialization

## How to use:

Clone the repo:
`git clone https://github.com/Cpaczek/classroom-initialization.git`

Install Dependencies:
`npm install`

Run the script:
`npm run start`
